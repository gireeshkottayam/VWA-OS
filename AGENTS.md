# VWA-OS Agent Contract

Inspect before editing. Use evidence rather than assumptions. Never treat a code change as proof of a visual fix. Verify rendered output, assets, console/network state and relevant responsive states. Keep changes reversible. Never expose secrets. Report residual risks honestly.
