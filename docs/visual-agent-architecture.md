# Visual Agent Architecture
The browser is an observable system. Source code is evidence, but rendered output is the truth for visual claims. Asset, browser and evidence agents cooperate; the Evidence Validator prevents unsupported 'fixed' claims.
