# Architecture

VWA-OS separates responsibilities into agents, skills, workflows, contracts and validation scripts.

Agents decide and coordinate. Skills provide repeatable procedures. Workflows define lifecycle. Contracts define machine-readable expectations. Scripts provide deterministic local checks.

The system is intentionally tool-agnostic so it can be adapted to OpenCode, custom agents, CI, or human workflows.
