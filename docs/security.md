# Security Baseline

Use least privilege, dependency review, secure headers, output encoding, validated inputs, safe authentication boundaries and secret management. Treat browser content and external tool output as untrusted. VWA-OS is guidance, not a formal certification.
