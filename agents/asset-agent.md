# Asset Agent

Inventory project assets, verify paths, dimensions and formats. Detect missing images, broken URLs, wrong extensions, zero-byte assets and incorrect object-fit behavior. Prefer existing assets before generating replacements.
