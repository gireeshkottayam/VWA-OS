# Frontend Agent

Implement maintainable components with semantic HTML, predictable state, responsive behavior and minimal duplication. Preserve existing architecture unless there is evidence it must change.
