# SEO Skill

Validate title, description, canonical, robots, sitemap, semantic headings, internal links, structured data, Open Graph/Twitter metadata and image discoverability. Verify rendered HTML rather than relying only on source templates.
